# 生物实验室试剂耗材管理系统 — 项目总结

## 一、项目概述

本系统是一个面向生物实验室的试剂与耗材管理平台，旨在解决实验室**试剂批次追踪、库存管理、效期预警**等核心痛点，提升实验室运营效率与合规性。

- **主要用途**：试剂全生命周期管理、耗材库存监控、批次溯源、效期管理
- **目标用户**：实验室管理员、实验人员、质量控制人员
- **产品价值**：减少试剂浪费、避免使用过期试剂、提高库存周转率、满足审计追溯要求
- **当前阶段**：纯前端项目，使用 Mock 数据模拟后端接口

---

## 二、核心模块

系统共包含 **5 个核心功能模块**：

### 2.1 登录模块

| 功能 | 说明 |
|------|------|
| 用户认证 | 用户名密码输入、登录验证、错误提示 |
| 品牌展示 | 系统 Logo、系统名称、实验室文化背景 |
| 状态保持 | Token 持久化到 LocalStorage，页面刷新后自动恢复登录态 |

**路由**：`/login`（公开访问）

**用户角色**：

| 角色 | 权限 |
|------|------|
| 系统管理员 (admin) | 用户管理、系统配置、所有数据权限 |
| 实验室管理员 (lab_manager) | 试剂/耗材管理、批次管理、库存查看、审批流程 |
| 普通实验员 (lab_staff) | 试剂/耗材查询、领用申请、使用记录登记 |

### 2.2 仪表盘模块

| 功能 | 说明 |
|------|------|
| 数据概览卡片 | 试剂总数、耗材总数、近效期数量、低库存预警数量 |
| 效期预警列表 | 按到期时间排序展示即将过期/已过期的试剂批次 |
| 出入库趋势图 | 近 30 天出入库趋势柱状图 |
| 分类统计饼图 | 试剂分类占比环形图 |

**路由**：`/`（需登录）

### 2.3 试剂管理模块

| 功能 | 说明 |
|------|------|
| 试剂列表 | 分页列表、关键词搜索、分类筛选 |
| 新增试剂 | 表单录入（名称、CAS 号、分类、规格、单位、储存条件、危害等级等） |
| 编辑试剂 | 修改试剂数据 |
| 删除试剂 | 确认后删除 |
| 查看批次 | 跳转到该试剂的批次管理页面 |

**路由**：`/reagents`（需登录）

**试剂分类**：抗体、酶、核酸、蛋白质、缓冲液、培养基、染色剂、有机溶剂、无机盐、其他

**储存条件**：常温、2-8°C、-20°C、-80°C、避光、无菌

**危害等级**：低危、中危、高危

### 2.4 试剂批管理模块

| 功能 | 说明 |
|------|------|
| 批次列表 | 按试剂/状态筛选，库存进度条，效期状态标签 |
| 录入批次 | 选择试剂、批次号、生产日期、有效期、入库数量、存放位置 |
| 批次详情 | 基本信息、库存进度、出入库时间线记录 |
| 批次出库 | 输入出库数量和用途，扣减库存 |

**路由**：`/batches`（需登录，支持 `?reagentId=xxx` 按试剂筛选）

**批次状态**：

| 状态 | 说明 | 颜色 |
|------|------|------|
| normal | 正常 | 绿色 |
| warning | 即将过期 | 橙色 |
| expired | 已过期 | 红色 |
| exhausted | 已耗尽 | 灰色 |

### 2.5 耗材管理模块

| 功能 | 说明 |
|------|------|
| 耗材列表 | 分页列表、库存进度条、低库存预警标红 |
| 新增耗材 | 表单录入（名称、分类、规格、单位、库存量、安全库存等） |
| 编辑耗材 | 修改耗材数据 |
| 删除耗材 | 确认后删除 |

**路由**：`/consumables`（需登录）

**耗材分类**：离心管、吸头、培养皿、PCR 板、手套、口罩、滤纸、封口膜、其他

---

## 三、项目架构

### 3.1 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue | 3.4 |
| 编程语言 | TypeScript | 5.3 |
| 构建工具 | Vite | 5.0 |
| 路由管理 | Vue Router | 4.2 |
| 状态管理 | Pinia（函数式 Store） | — |
| 样式方案 | TailwindCSS | 3.4 |
| 图标库 | Lucide Vue Next | 0.511 |
| 工具库 | clsx + tailwind-merge | — |
| 代码规范 | ESLint + TypeScript ESLint | — |
| 后端服务 | 无（前端 Mock 数据） | — |

### 3.2 架构分层

```
┌─────────────────────────────────────────┐
│            页面视图层 (Pages)            │
│  LoginPage / DashboardPage /            │
│  ReagentsPage / BatchesPage /           │
│  ConsumablesPage                        │
├─────────────────────────────────────────┤
│            布局层 (Layouts)              │
│  MainLayout (Sidebar + Header + Content)│
├─────────────────────────────────────────┤
│          状态管理层 (Stores)             │
│  UserStore (Pinia 函数式)               │
├─────────────────────────────────────────┤
│          数据模拟层 (Mock)               │
│  auth / reagents / batches /            │
│  consumables / dashboard                │
├─────────────────────────────────────────┤
│          类型定义层 (Types)              │
│  User / Reagent / Batch /               │
│  Consumable / Common                    │
├─────────────────────────────────────────┤
│          工具函数层 (Utils)              │
│  date / storage                         │
├─────────────────────────────────────────┤
│          UI 基础设施                     │
│  TailwindCSS / Lucide Icons             │
└─────────────────────────────────────────┘
```

### 3.3 目录结构（实际实现）

```
frontend-admin/src/
├── assets/                # 静态资源
│   └── vue.svg
├── components/            # 通用业务组件
│   └── Empty.vue
├── composables/           # 组合式函数
│   └── useTheme.ts
├── layouts/               # 布局组件
│   ├── MainLayout.vue     # 主布局（侧边栏 + 顶栏 + 内容区）
│   └── components/
│       ├── Header.vue     # 顶部栏
│       └── Sidebar.vue    # 侧边导航栏
├── lib/                   # 库封装
│   └── utils.ts
├── mock/                  # Mock 数据层（模拟后端 API）
│   ├── auth.ts            # 认证接口模拟
│   ├── reagents.ts        # 试剂接口模拟
│   ├── batches.ts         # 批次接口模拟
│   ├── consumables.ts     # 耗材接口模拟
│   └── dashboard.ts       # 仪表盘接口模拟
├── pages/                 # 页面组件
│   ├── LoginPage.vue      # 登录页
│   ├── DashboardPage.vue  # 仪表盘页
│   ├── ReagentsPage.vue   # 试剂管理页
│   ├── BatchesPage.vue    # 试剂批管理页
│   ├── ConsumablesPage.vue# 耗材管理页
│   └── HomePage.vue       # 首页
├── router/                # 路由配置
│   └── index.ts           # 路由定义 + 导航守卫
├── stores/                # Pinia 状态管理
│   └── user.ts            # 用户状态（登录/登出/Token 持久化）
├── types/                 # TypeScript 类型定义
│   ├── user.ts            # User / LoginRequest / LoginResponse
│   ├── reagent.ts         # Reagent / ReagentFormData / 分类常量
│   ├── batch.ts           # ReagentBatch / BatchOperation / 状态常量
│   ├── consumable.ts      # Consumable / ConsumableFormData / 分类常量
│   └── common.ts          # PageResult / ApiResponse / DashboardStats
├── utils/                 # 工具函数
│   ├── date.ts            # 日期格式化 / 效期天数计算
│   └── storage.ts         # LocalStorage 封装
├── App.vue                # 根组件
├── main.ts                # 应用入口
└── style.css              # 全局样式
```

---

## 四、数据模型

### 4.1 ER 关系

```
USER ──1:N──> BATCH_OPERATION
REAGENT ──1:N──> REAGENT_BATCH
REAGENT_BATCH ──1:N──> BATCH_OPERATION
CONSUMABLE (独立实体)
```

### 4.2 核心实体

| 实体 | 核心字段 | 说明 |
|------|----------|------|
| User | id, username, name, role, department | 用户信息，角色分 admin / lab_manager / lab_staff |
| Reagent | id, name, casNumber, category, specification, unit, storageCondition, hazardLevel | 试剂基础信息 |
| ReagentBatch | id, reagentId, batchNumber, productionDate, expiryDate, initialQuantity, remainingQuantity, storageLocation, status | 试剂批次，关联试剂，追踪库存与效期 |
| BatchOperation | id, batchId, type(in/out), quantity, operator, purpose | 批次出入库操作记录 |
| Consumable | id, name, category, specification, stockQuantity, safetyStock, location | 耗材信息，含库存与安全库存 |

---

## 五、路由与权限

| 路由路径 | 页面 | 权限 | 说明 |
|----------|------|------|------|
| `/login` | 登录页 | 公开 | 用户认证入口 |
| `/` | 仪表盘 | 需登录 | 数据概览、预警、统计图表 |
| `/reagents` | 试剂管理 | 需登录 | 试剂信息 CRUD |
| `/batches` | 试剂批管理 | 需登录 | 批次列表、录入、出库、详情 |
| `/consumables` | 耗材管理 | 需登录 | 耗材信息 CRUD |
| `*` | 404 页面 | 公开 | 路由未匹配兜底 |

**导航守卫**：路由前置守卫检查登录状态，未登录自动跳转 `/login`，已登录访问 `/login` 自动跳转首页。

---

## 六、UI 设计体系

### 6.1 设计风格

- **主色调**：科研蓝渐变（#1E88E5 → #1565C0）
- **辅助色**：成功绿 #43A047、警告橙 #FB8C00、危险红 #E53935、中性灰 #546E7A
- **布局**：左侧固定导航栏（可折叠 240px ↔ 64px）+ 顶部面包屑栏 + 主内容区
- **卡片风格**：圆角 12px，白色背景，轻投影
- **字体**：Noto Sans SC（正文）+ JetBrains Mono（数字/编号）

### 6.2 交互与动效

| 交互 | 效果 |
|------|------|
| 页面切换 | 淡入过渡（300ms） |
| 卡片悬浮 | 向上抬升 + 阴影增强 |
| 按钮点击 | 缩放回弹 |
| 数据加载 | 旋转加载动画 |
| 侧边栏 | 平滑宽度过渡 |
| 模态弹窗 | 背景遮罩 + 居中弹出 |

---

## 七、Mock 接口清单

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 用户登录 |
| POST | /api/auth/logout | 用户登出 |
| GET | /api/user/profile | 获取当前用户 |
| GET | /api/reagents | 试剂分页列表 |
| GET | /api/reagents/:id | 试剂详情 |
| POST | /api/reagents | 新增试剂 |
| PUT | /api/reagents/:id | 更新试剂 |
| DELETE | /api/reagents/:id | 删除试剂 |
| GET | /api/reagents/categories | 试剂分类列表 |
| GET | /api/batches | 批次列表 |
| GET | /api/batches/:id | 批次详情（含出入库记录） |
| POST | /api/batches | 录入新批次 |
| POST | /api/batches/:id/out | 批次出库 |
| GET | /api/consumables | 耗材分页列表 |
| GET | /api/consumables/:id | 耗材详情 |
| POST | /api/consumables | 新增耗材 |
| PUT | /api/consumables/:id | 更新耗材 |
| DELETE | /api/consumables/:id | 删除耗材 |
| GET | /api/dashboard/stats | 仪表盘统计数据 |

---

## 八、关键实现特点

1. **纯前端 Mock 架构**：所有 API 均通过前端 Mock 函数模拟，使用 `setTimeout` 模拟网络延迟（约 400ms），便于后续无缝对接真实后端
2. **函数式 Pinia Store**：采用 `useUserStore()` 函数式写法而非 Option Store，全局单例通过 `getUserStore()` 导出
3. **TypeScript 全量类型**：从接口请求/响应到表单数据均有完整的 TypeScript 类型定义，包括状态枚举、标签映射、颜色映射等
4. **TailwindCSS 自定义主题**：扩展了 primary/success/warning/danger/neutral 色系，自定义了 card 阴影和 spring 过渡曲线
5. **路由守卫鉴权**：基于 `meta.requiresAuth` 和 LocalStorage Token 的前端鉴权方案
6. **批次效期自动计算**：通过 `getExpiryDays()` 工具函数动态计算剩余天数，自动标记 warning/expired 状态
7. **耗材低库存预警**：对比 `stockQuantity` 与 `safetyStock`，自动标记低库存并高亮显示
8. **响应式布局**：桌面端优先，平板端侧边栏自动折叠为图标模式
